name: "Deploy pages"

permissions:
  contents: write
  id-token: write
  pages: write
on:
  workflow_dispatch:

jobs:
  dev:
    uses: ./.github/workflows/deploy_branch.yml
    with:
      dev: true
    permissions:
      contents: write
      id-token: write

  master:
    uses: ./.github/workflows/deploy_branch.yml
    with:
      dev: false
    permissions:
      contents: write
      id-token: write

  deploy:
    runs-on: ubuntu-latest
    needs: [dev, master]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download MDBook artifacts (dev)
        uses: actions/download-artifact@v5
        with:
          artifact-ids: ${{ needs.dev.outputs.mdbook_artifact_id }}
          path: site/dev/book
      - name: Download rustdoc artifacts (dev)
        uses: actions/download-artifact@v5
        with:
          artifact-ids: ${{ needs.dev.outputs.rustdoc_artifact_id }}
          path: site/dev/doc

      - name: Download MDBook artifacts (master)
        uses: actions/download-artifact@v5
        with:
          artifact-ids: ${{ needs.master.outputs.mdbook_artifact_id }}
          path: site/book
      - name: Download rustdoc artifacts (master)
        uses: actions/download-artifact@v5
        with:
          artifact-ids: ${{ needs.master.outputs.rustdoc_artifact_id }}
          path: site/doc
        
      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4